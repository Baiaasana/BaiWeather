<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BaiWeather" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="colorPrimary">@color/primary_text</item>
        <item name="colorPrimaryDark">@color/background_medium</item>
        <item name="colorAccent">@color/background_primary</item>
        <item name="android:windowBackground">@color/background_medium</item>
    </style>

    <style name="Theme.BaiWeather.Night" parent="Base.Theme.BaiWeather" />

</resources>